import DeprecatedBadge from '@site/src/components/DeprecatedBadge.js';

# Main API's
## test_api
```cpp
test_api() -> void
```
Prints ```[INFO-LUA] test_api! ```
## isDiscordRunning
```cpp
isDiscordRunning() -> bool
```
Return's if discord is running
## updDiscordActivity
```cpp
updDiscordActivity(
    details : string,
    state : string,
    startTimestamp : number, 
    endTimestamp : number,
    assetLarge : string,
    assetSmall : string,
    largeImgText : string,
    smallImageText : string,
    button1Text : string,
    button2Text : string,
    button1url : string,
    button2url : string,
) -> void
```
Updates the users discord activity
## createNotification
```cpp
createNotification(title : string, message : string, timeout : number) -> void
```
Creates a notification with a timeout
## runCommand
<DeprecatedBadge type="dangerous" customMessage="This command is very dangerous">
</DeprecatedBadge>
```cpp
runCommand(command : string) -> void
```
Runs a system command.
## getDataFromURL
```cpp
getDataFromURL(url : string) -> string
```
Get's data from a url
## decodeJSON
```cpp
decodeJSON(json : string) -> {[name : string], [value : any]}
```
Decodes a JSON string (will be replaced in v1.0.1)
